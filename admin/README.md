# Aura Diet Admin

```bash
npm install
npm run dev
```

Use `.env.example` as the starting point for `.env.local`. Keep the Supabase service role key private and deploy the admin behind SSO or private network access before production use.
